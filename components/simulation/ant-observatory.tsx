'use client';
import { useRef,useState,useEffect } from 'react';
import HubNavigation, { SimulatorId } from '@/components/hub-navigation';
import { Activity, ArrowDownToLine, ArrowUpFromLine, Bug, ChevronDown, ChevronRight, CircleHelp, Droplets, Focus, Layers3, Leaf, Minus, MousePointer2, Mountain, Orbit, Pause, Play, Plus, RotateCcw, Save, Settings2, SlidersHorizontal, Sprout, Sun, Target, Thermometer, X } from 'lucide-react';
import { Sidebar,SidebarProvider,SidebarHeader,SidebarContent,SidebarFooter,SidebarTrigger,SidebarMenu,SidebarMenuItem,SidebarMenuButton } from '@/components/ui/sidebar';
import { Slider } from '@/components/ui/slider';
import { Switch } from '@/components/ui/switch';
import { Tabs,TabsList,TabsTrigger } from '@/components/ui/tabs';
import { Dialog,DialogContent,DialogHeader,DialogTitle,DialogDescription } from '@/components/ui/dialog';
import { Select,SelectTrigger,SelectValue,SelectContent,SelectItem } from '@/components/ui/select';
import WorldCanvas,{CanvasControls} from '@/components/simulation/world-canvas';
import { useSimulation } from '@/lib/simulation/use-simulation';
import { registerSimulationTools } from '@/lib/simulation/webmcp';
import SaveDialog from '@/components/simulation/save-dialog';
import Inspector from '@/components/simulation/inspector';
import { Toaster } from '@/components/ui/sonner';
import { AlertDialog,AlertDialogContent,AlertDialogHeader,AlertDialogTitle,AlertDialogDescription,AlertDialogFooter,AlertDialogCancel,AlertDialogAction } from '@/components/ui/alert-dialog';
import { Command,Settings,View } from '@/lib/simulation/types';

export default function AntObservatory({active,onNavigate}:{active:boolean;onNavigate:(id:SimulatorId)=>void}){
 const {snapshot,liveSnapshot,dispatch,ready,error,savedAt}=useSimulation();const {state,paused}=snapshot;const speed=String(snapshot.speed);
 const [view,setView]=useState<View>('surface');const [pheromones,setPheromones]=useState(false);const [activity,setActivity]=useState(false);const [conditions,setConditions]=useState(false);const [selected,setSelected]=useState<number|null>(null);const [follow,setFollow]=useState(false);const [tool,setTool]=useState('select');const [statsOpen,setStatsOpen]=useState(true);const [help,setHelp]=useState(false);const [saveOpen,setSaveOpen]=useState(false);const [resetOpen,setResetOpen]=useState(false);const [fps,setFps]=useState(60);const [renderMs,setRenderMs]=useState(0);const canvasControls=useRef<CanvasControls|null>(null);
 const ant=state.ants.find(a=>a.id===selected);const workers=state.ants.filter(a=>a.alive&&a.task!=='queen');const act=(command:Command)=>{void dispatch(command).catch(()=>{});};const patch=(key:keyof Settings,value:number)=>act({type:'settings',settings:{[key]:value}});const setPaused=(value:boolean)=>act({type:'pause',paused:value});const setSpeed=(value:string)=>act({type:'speed',speed:Number(value)});
 useEffect(()=>active?registerSimulationTools(dispatch):undefined,[dispatch,active]);
 const resumeOnReturn=useRef(false);const wasActive=useRef(active);
 useEffect(()=>{if(wasActive.current&&!active){resumeOnReturn.current=!paused;void dispatch({type:'pause',paused:true}).catch(()=>{});}else if(!wasActive.current&&active&&resumeOnReturn.current){void dispatch({type:'pause',paused:false}).catch(()=>{});}wasActive.current=active;},[active,dispatch,paused]);
 useEffect(()=>{if(!active)return;const key=(e:KeyboardEvent)=>{const target=e.target as HTMLElement;if(target.closest('input,textarea,button,[role="dialog"],[role="slider"],[role="combobox"],[contenteditable="true"]'))return;if(e.code==='Space'){e.preventDefault();void dispatch({type:'pause',paused:!paused}).catch(()=>{});}if(e.key==='Escape'){setTool('select');setSelected(null);setFollow(false);}};window.addEventListener('keydown',key);return()=>window.removeEventListener('keydown',key);},[dispatch,paused,active]);
 useEffect(()=>{if(follow&&ant)setView(ant.view);},[follow,ant?.view]);
 const elapsed=new Date(state.time*1000).toISOString().slice(11,19);const onPlace=(x:number,y:number)=>{if(view==='surface'&&tool!=='select')act({type:'place',kind:tool as 'carbohydrate'|'protein'|'water'|'obstacle'|'erase',x,y});};
 return <SidebarProvider style={{'--sidebar-width':'264px'} as React.CSSProperties} className="app-shell"><Sidebar className="observatory-sidebar"><SidebarHeader className="brand"><div className="brand-symbol"><Orbit size={23}/></div><div><strong>simulation<span>hub</span></strong><small>A SPACE FOR CURIOSITY</small></div><SidebarTrigger className="mobile-sidebar-close" aria-label="Close settings"/></SidebarHeader><SidebarContent><HubNavigation current="ants" onNavigate={onNavigate}/><div className="sidebar-divider"/><div className="sidebar-section settings-section"><div className="section-title"><SlidersHorizontal size={15}/><span>Environment</span></div><Parameter label="Temperature" icon={<Thermometer size={14}/>} value={state.settings.temperature} display={`${state.settings.temperature}°C`} min={5} max={40} onChange={v=>patch('temperature',v)}/><Parameter label="Soil moisture" icon={<Droplets size={14}/>} value={state.settings.moisture} display={`${state.settings.moisture}%`} min={5} max={95} onChange={v=>patch('moisture',v)}/><div className="environment-note"><Sun size={15}/><span>Temperate woodland</span></div></div><div className="sidebar-divider"/><div className="sidebar-section"><div className="section-title"><Layers3 size={15}/><span>World layers</span></div><Toggle label="Pheromone trails" checked={pheromones} onChange={setPheromones} color="mint"/><Toggle label="Colony activity" checked={activity} onChange={setActivity} color="amber"/><Toggle label="Nest conditions" checked={conditions} onChange={setConditions} color="blue"/></div><div className="sidebar-divider"/><div className="sidebar-section"><details className="behavior-settings"><summary><Settings2 size={15}/><span>Behavior settings</span><ChevronDown size={14}/></summary><Parameter label="Life-cycle rate" value={state.settings.lifecycle} display={`${state.settings.lifecycle}×`} min={.25} max={4} step={.25} onChange={v=>patch('lifecycle',v)}/><Parameter label="Exploration" value={state.settings.exploration} display={`${Math.round(state.settings.exploration*100)}%`} min={0} max={1} step={.05} onChange={v=>patch('exploration',v)}/><Parameter label="Trail persistence" value={.06-state.settings.pheromoneDecay} display={`${Math.round(Math.log(2)/state.settings.pheromoneDecay)}s`} min={.005} max={.057} step={.001} onChange={v=>patch('pheromoneDecay',Number((.06-v).toFixed(3)))}/><Parameter label="Scent sensitivity" value={state.settings.sensitivity} display={`${state.settings.sensitivity.toFixed(1)}×`} min={.2} max={3} step={.1} onChange={v=>patch('sensitivity',v)}/><Parameter label="Task flexibility" value={state.settings.flexibility} display={`${Math.round(state.settings.flexibility*100)}%`} min={.1} max={1} step={.1} onChange={v=>patch('flexibility',v)}/><p className="muted small">Development and aging use accelerated sandbox timescales.</p></details></div></SidebarContent><SidebarFooter className="sidebar-bottom"><button onClick={()=>setHelp(true)}><CircleHelp size={17}/>Field notes<span>↗</span></button><div className="version-line"><span>ANT OBSERVATORY</span><span>v1.0</span></div></SidebarFooter></Sidebar>
 <main className="workspace"><header className="topbar"><div className="breadcrumb"><SidebarTrigger/><span>Simulations</span><ChevronRight size={14}/><strong>Ant colony</strong></div><div className="header-actions"><span className="local-label">LOCAL SANDBOX</span><button className="button secondary" onClick={()=>setSaveOpen(true)}><Save size={15}/><span>Save colony</span></button></div></header><div className="page-heading"><div><div className="title-row"><h1>Small lives. <span>Complex worlds.</span></h1><span className="species-tag">LASIUS NIGER</span></div><p>A living colony, one decision at a time.</p></div><button className="icon-button subtle" title="About this simulation" onClick={()=>setHelp(true)}><CircleHelp size={18}/></button></div>
 {error&&<div className="error-banner" role="alert">{error}</div>}<section className="simulation-panel" aria-label="Ant colony simulation"><div className="world-topbar"><Tabs value={view} onValueChange={v=>{setView(v as View);setTool('select');}}><TabsList className="view-tabs"><TabsTrigger value="surface"><Sprout size={15}/>Surface</TabsTrigger><TabsTrigger value="nest"><Layers3 size={15}/>Nest</TabsTrigger></TabsList></Tabs><div className="playback"><span className="elapsed" title="Simulation elapsed time">{elapsed}</span><span className="toolbar-divider"/><button className={`icon-button pause-button ${paused?'is-paused':''}`} title={paused?'Resume simulation (Space)':'Pause simulation (Space)'} onClick={()=>setPaused(!paused)}>{paused?<Play size={15}/>:<Pause size={15}/>}</button><Select value={speed} onValueChange={setSpeed}><SelectTrigger className="speed-select" aria-label="Simulation speed"><SelectValue/></SelectTrigger><SelectContent>{['1','2','4','8'].map(s=><SelectItem key={s} value={s}>{s}×</SelectItem>)}</SelectContent></Select><button className="icon-button" title="Reset colony" onClick={()=>setResetOpen(true)}><RotateCcw size={15}/></button></div></div>
 <div className="world-area"><WorldCanvas state={state} liveSnapshot={liveSnapshot} view={view} pheromones={pheromones} activity={activity} conditions={conditions} selected={selected} follow={follow} onSelect={setSelected} onPlace={onPlace} tool={tool} controls={canvasControls} onPerformance={(f,ms)=>{setFps(f);setRenderMs(ms);}}/><div className="habitat-caption"><span className="live-indicator"/><span>{paused?'OBSERVATION PAUSED':'COLONY OBSERVATION'}</span><span className="caption-divider"/>{view==='surface'?'WOODLAND FLOOR':'BELOW THE SURFACE'}</div><div className="world-tools" role="toolbar" aria-label="Environment tools">{[{id:'select',icon:MousePointer2,label:'Select and inspect'},{id:'carbohydrate',icon:Leaf,label:'Place carbohydrate food'},{id:'protein',icon:Bug,label:'Place protein food'},{id:'water',icon:Droplets,label:'Place water'},{id:'obstacle',icon:Mountain,label:'Place rock'},{id:'erase',icon:X,label:'Remove resource or rock'}].map(t=><button key={t.id} className={tool===t.id?'active':''} aria-label={t.label} title={t.label} aria-pressed={tool===t.id} disabled={view==='nest'&&t.id!=='select'} onClick={()=>setTool(t.id)}><t.icon size={18}/></button>)}</div>
 <div className="world-hint">{tool==='select'?<><MousePointer2 size={13}/> Select an ant to discover its world</>:<>Click the surface to {tool==='erase'?'remove a resource or rock':`place ${tool==='carbohydrate'?'nectar':tool==='obstacle'?'a rock':tool}`}</>}</div><div className="zoom-controls"><button aria-label="Zoom in" onClick={()=>canvasControls.current?.zoom(1.3)}><Plus size={17}/></button><button aria-label="Zoom out" onClick={()=>canvasControls.current?.zoom(1/1.3)}><Minus size={17}/></button><span/><button aria-label="Fit world" onClick={()=>canvasControls.current?.fit()}><Focus size={17}/></button></div>
 {ant&&<Inspector ant={ant} follow={follow} onFollow={()=>setFollow(!follow)} onClose={()=>{setSelected(null);setFollow(false);}} onView={setView}/>}
 <div className="canvas-footer"><span><span className="scale-line"/> Illustrative scale</span><span title={`Render ${renderMs.toFixed(1)} ms/frame · Simulation ${snapshot.stepMs.toFixed(2)} ms/tick`}>{fps} FPS <span className="fps-dot"/></span></div></div>
 <div className="colony-overview"><button className="overview-title" onClick={()=>setStatsOpen(!statsOpen)} aria-expanded={statsOpen}><Activity size={15}/><span>Colony overview</span><ChevronDown size={14} className={statsOpen?'':'flipped'}/></button><span className="overview-note">Every ant has a part to play.</span></div>{statsOpen&&<div className="metrics-grid"><Metric label="WORKERS" value={workers.length.toString()} note={`${state.births} emerged · ${state.deaths} deaths`} history={state.history.map(h=>h.population)} icon={<Bug size={15}/>} color="amber"/><Metric label="BROOD" value={state.brood.length.toString()} note={`${state.brood.filter(b=>b.stage==='egg').length} eggs · ${state.brood.filter(b=>b.stage==='larva').length} larvae · ${state.brood.filter(b=>b.stage==='pupa').length} pupae`} history={state.history.map(h=>h.brood)} icon={<Sprout size={15}/>} color="lavender"/><Metric label="FOOD RESERVES" value={`${Math.round(state.stores.carbohydrate+state.stores.protein)}`} note="Carbohydrate + protein" history={state.history.map(h=>h.food)} icon={<Leaf size={15}/>} color="mint"/><Metric label="OUT FORAGING" value={workers.filter(a=>a.view==='surface').length.toString()} note="Exploring the surface" history={state.history.map(h=>h.foraging)} icon={<Activity size={15}/>} color="blue"/></div>}</section><footer className="workspace-footer"><span><span className="status-dot"/> {error?'Needs attention':!ready?'Connecting to the colony…':savedAt?'Autosaved on this device':'Colony connected'}</span><span>Drag to explore <i>·</i> Scroll to zoom <i>·</i> Space to pause</span></footer>
 </main><Dialog open={help} onOpenChange={setHelp}><DialogContent className="notes-dialog"><DialogHeader><DialogTitle>Inside the ant observatory</DialogTitle><DialogDescription>A sandbox inspired by the black garden ant.</DialogDescription></DialogHeader><p>Explore a colony above and below ground. Add food, change the environment, and watch individual choices become collective behavior.</p><div className="note-block"><h3>Look a little closer</h3><p>Click an ant to inspect it. Enable pheromone trails to reveal chemical communication. Surface and Nest show the same colony.</p></div><div className="note-block"><h3>Biology, with room to experiment</h3><p>Development and aging are accelerated. Temperature, moisture, nutrition, and local needs influence the colony. This is an explanatory model, not a calibrated prediction of real colonies.</p></div><p className="muted small">Use the arrow keys to pan the focused canvas, +/− to zoom, Enter to inspect an ant, and Space to pause.</p></DialogContent></Dialog><SaveDialog open={saveOpen} onOpenChange={setSaveOpen} dispatch={dispatch}/><AlertDialog open={resetOpen} onOpenChange={setResetOpen}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Start this colony again?</AlertDialogTitle><AlertDialogDescription>The current colony will be replaced with its original seeded world. Named saves are kept. Save first if you want to return to this moment.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Keep observing</AlertDialogCancel><AlertDialogAction onClick={()=>{act({type:'reset',seed:state.seed});setSelected(null);setFollow(false);}}>Reset colony</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog><Toaster theme="dark" position="bottom-right" richColors/></SidebarProvider>;
}
function Parameter({label,icon,value,display,min,max,step=1,onChange}:{label:string;icon?:React.ReactNode;value:number;display:string;min:number;max:number;step?:number;onChange:(n:number)=>void}){return <div className="parameter"><div><label>{icon}{label}</label><output>{display}</output></div><Slider aria-label={label} aria-labelledby={undefined} value={[value]} min={min} max={max} step={step} onValueChange={v=>onChange(v[0])}/></div>}
function Toggle({label,checked,onChange,color}:{label:string;checked:boolean;onChange:(v:boolean)=>void;color:string}){return <label className="layer-toggle"><span className={`layer-swatch ${color}`}/><span>{label}</span><Switch aria-label={label} checked={checked} onCheckedChange={onChange}/></label>}
function Metric({label,value,note,icon,color,history}:{label:string;value:string;note:string;icon:React.ReactNode;color:string;history:number[]}){const data=history.slice(-90);const min=Math.min(...data)*.9,max=Math.max(...data)*1.1,range=max-min||1;const points=data.map((n,i)=>`${i/Math.max(1,data.length-1)*100},${30-(n-min)/range*26}`).join(' ');return <div className={`metric ${color}`}><div className="metric-label">{icon}{label}</div><div className="metric-value">{value}{data.length>1?<svg className="sparkline" viewBox="0 0 100 34" role="img" aria-label={`${label.toLowerCase()} history over the last ${Math.min(data.length*5,450)} simulation seconds`}><polyline points={points} fill="none" stroke="currentColor" strokeWidth="1.6" vectorEffect="non-scaling-stroke"/></svg>:<span className="metric-rule"/>}</div><div className="metric-note">{note}</div></div>}
